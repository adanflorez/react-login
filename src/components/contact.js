const contact = {
  height: "50px",
};

const Contact = () => {
  return (
    <div
      style={contact}
      className="d-flex justify-content-center align-items-center bg-primary"
    >
      Contáctanos 000-000-0000
    </div>
  );
};

export default Contact;
